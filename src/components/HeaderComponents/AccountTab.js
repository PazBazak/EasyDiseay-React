import React from "react";

const AccountTab = () => {
    return (
        <div className={'ml-auto'}>
            <input id={'login'}
                   className="btn border"
                   type="button"
                   value="Account"/>
        </div>
    )
};

export default AccountTab;
