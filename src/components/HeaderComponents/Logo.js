import React from "react";

/**
 * The logo of the site.
 * The logo is an <a> tag so it can be clickable.
 * */
function Logo() {
    return (
        <a className="logo" href="#">
            EasyDeasy
        </a>
        //Add brand image in the future (possibly with animation)
    )
}

export default Logo;

